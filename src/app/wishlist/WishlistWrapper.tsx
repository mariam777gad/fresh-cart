// import { createContext, useState } from "react";

// export const wishlistWrapper = createContext({
//   inCart: false,
//   setInCart: (exist: boolean) => {},
// });
// export default function WishlistWrapper({
//   children,
// }: {
//   children: React.ReactNode;
// }) {
//   const [inCart, setInCart] = useState(false);
//   return (
//     <wishlistWrapper.Provider value={{inCart, setInCart}}>{children}</wishlistWrapper.Provider>
//   );
// }
